import 'package:flutter_test/flutter_test.dart';

import 'package:peoplefirst_mobile/main.dart';

void main() {
  test('App shell can be constructed', () {
    expect(const PeopleFirstApp(), isA<PeopleFirstApp>());
  });
}
